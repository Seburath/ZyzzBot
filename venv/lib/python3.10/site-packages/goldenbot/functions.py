#!/usr/bin/env python3


def move_decimals(int_money):
    firstchar = "-" if int_money < 0 else ""
    s = str(abs(int_money))

    if len(s) == 1:
        s = "00" + s
    if len(s) == 2:
        s = "0" + s
    return firstchar + s[:-2] + "." + s[-2:]
